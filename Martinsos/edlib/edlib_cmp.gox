compareVer (a, b) => {
	aRelease := semver.isValid(a.Version)
	bRelease := semver.isValid(b.Version)
	if aRelease && bRelease {
		return semver.compare(a.Version, b.Version)
	}
	if aRelease {
		return 1
	}
	if bRelease {
		return -1
	}
	return gnu.compare(a.Version, b.Version)
}
